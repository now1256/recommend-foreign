# 3단 파이프라인 설계 — 외국인에게 지방 코스를 추천하기

```
외국인 특성  ──1단──▶  시도 추천  ──2단──▶  시작 POI  ──3단──▶  코스 5곳
(국적·연령·목적)      (14개 중)        (그 시도 안)       (지리+유형 고려)
```

각 단이 **쓰는 데이터·학습 방식·검증 가능성이 전부 다르다.** 이걸 섞으면 안 된다.

---

## 1단. 외국인 특성 → 시도

| | 내용 |
|---|---|
| **입력** | 국적(21) · 연령 · 성별 · 재방문 · 여행형태 · 방한목적 · 체재기간 · 동반 |
| **출력** | 지방 14개 시도 점수 |
| **학습 데이터** | 외래관광객조사 16,110명 × 14 = 225,540행, positive 7,124 |
| **라벨** | 문9-2 방문 여부 (0/1) |
| **모델** | Two-Tower (콘텐츠 기반, 아이템 ID 임베딩 없음) |
| **검증** | ✅ **가능** — 사용자 분할 Recall@k |

**성능** (`project` 1차에서 확립)

| 구분 | R@1 | R@3 | R@5 |
|---|---|---|---|
| 전체 14개 · 모델 | 0.662 | 0.846 | 0.901 |
| 전체 14개 · 인기순 | 0.508 | 0.832 | 0.875 |
| **부산·제주 제외 · 모델** | **0.466** | 0.703 | 0.808 |
| 부산·제주 제외 · 인기순 | 0.359 | 0.607 | 0.722 |

**이 단만 외국인 라벨로 직접 검증된다.**

---

## 2단. 시도 → 시작 POI  ★ 설계 쟁점

시도가 정해졌을 때 그 안의 어느 POI에서 시작할 것인가.

### 왜 어려운가

**외국인의 POI 단위 정답이 없다.** 외래관광객조사 문9-1("가장 좋았던 곳")이 유일한 후보였으나:

```
지방 POI 응답 4,590명 / 285종
AI Hub POI명 매칭률 10.3%
미매칭 사유: "제주도", "전주", "부산시" 등 POI가 아니라 지역명
```

→ **문9-1로는 검증할 수 없다.** (실측 확인)

### 선택지 3가지

| 방식 | 학습 | 외국인 특성 사용 | 검증 |
|---|---|---|---|
| **A. 내국인 CF 전이** | AI Hub 행동 | ✗ | 내국인으로만 |
| **B. 외국인 특성 + POI 속성** | 외국인 라벨 없음 | ✓ | ✗ 불가 |
| **C. 카드 소비 국적 매칭** | 학습 없음(집계) | ✓ (국적만) | ✗ 불가 |

**A 채택 이유**: 유일하게 검증 가능한 근거를 가진다.
`project`의 실험 3(사람 특성 무신호, 리프트 1.10배)에 따라 B는 기대값이 낮다.
C는 신한카드 국적×시군구 소비로 **외국인 접근성 보정**에만 쓴다(검증 없이 규칙으로 명시).

### 채택 설계

```
시작 POI 점수 = EASE_인기도(그 시도 내)          ← 내국인 행동 (검증됨)
              × 외국인_접근성(국적, 시군구)        ← 카드 소비 (미검증, 규칙)
              × 유형_적합도(1단 특성과 매칭)       ← 선택적
```

**외국인 특성은 여기서 "필터·가중"으로만 쓴다.** 학습에 넣지 않는 이유는 라벨이 없어서다.

---

## 3단. 시작 POI → 코스 5곳

| | 내용 |
|---|---|
| **입력** | 시작 POI + 시도 |
| **출력** | 5곳 코스 (순서 포함) |
| **모델** | EASE + 지리 근접 + 유형 다양성 (탐욕 선택) |
| **검증** | △ **부분 가능** — 내국인 실제 여정과 비교 |

### 점수 함수

```
score(후보 i | 선택된 route) =
      2.0 × EASE관련성(route 전체와의 평균)     ← 연쇄가 아니라 집합 기준
    + 1.0 × 근접도(1 − 최단거리/25km)
    + 0.5 × 유형다양성(아직 안 나온 유형이면)
    + 0.1 × 평균만족도
```

**"연쇄가 똑같아지는" 문제 해결**: 관련성을 직전 POI가 아니라 **선택된 전체와의 평균**으로 계산.
시작점이 바뀌면 코스 전체가 달라진다.

### 실제 출력 예

```
[경북] 첨성대(역사) → 핑크뮬리군락지(자연) 0.1km → 교촌마을(산책) 0.6km
                   → 경주중앙시장(상업) 1.7km → 동궁식물원(테마) 5.1km

[강원] 속초관광수산시장(상업) → 속초해수욕장(자연) 2.0km → 속초아이(체험) 0.1km
                            → 영랑호(산책) 3.6km → 낙산사(역사) 11.1km
```

---

## 전체 파이프라인 검증 — 정직한 정리

| 단계 | 검증 방법 | 상태 |
|---|---|---|
| 1단 | 외국인 문9-2로 Recall@k | ✅ R@1 0.466 (인기순 0.359) |
| 2단 | 내국인 EASE HR@k | ✅ HR@1 0.200 / HR@10 0.678 (인기순 0.151) |
| 2단 (외국인 전이) | — | ❌ **정답 없음** |
| 3단 (코스 관련성) | 내국인 실제 여정과 비교 | △ **미실시, 가능** |
| 3단 (외국인 적합성) | — | ❌ **정답 없음** |
| **1+2+3 통합** | — | ❌ **불가능** |

### 통합 검증이 왜 불가능한가

외국인의 **POI 단위 실제 방문 기록이 존재하지 않는다.**
- 외래관광객조사: 시도까지
- 신한카드: 개인 ID 없음
- AI Hub: 내국인

세 축(국적 × POI × 개인)을 모두 가진 공개 데이터는 확인되지 않았다.

### 그래서 어떻게 주장할 것인가

**단계별로 검증 수준을 명시해서 제시한다.** 통합 성능을 주장하지 않는다.

> "1단은 외국인 데이터로 검증됐다(R@1 0.466).
> 2·3단은 내국인 행동 데이터로 검증됐다(HR@10 0.678, 인기순의 4.5배).
> 두 단을 잇는 **전이는 가정이며, 검증되지 않았다.**"

이게 `project` README의 *"결과 해석 시 반드시 구분할 것"* 원칙과 같다.

### 통합 검증을 가능하게 하려면

| 필요한 것 | 획득 가능성 |
|---|---|
| 외국인 POI 방문 이력 | 카드 가맹점 단위 + 개인 ID → **대회 데이터로는 불가** |
| 외국인 대상 A/B 테스트 | 실제 서비스 필요 |
| 3단 내국인 여정 비교 | ✅ **지금 가능** — 미실시 |

**당장 할 수 있는 유일한 추가 검증은 3단의 내국인 여정 비교**다.
실제 여행에서 A 다음에 간 곳을, 우리 코스 생성기가 상위에 놓는지 재는 것.

---

## 대안 설계 — 검증 부담을 줄이는 방법

1단과 2·3단을 **하나의 추천으로 합치지 않고 분리 제시**하는 방법도 있다.

```
산출물 A: "어느 지방으로 유도할 것인가"     ← 1단, 외국인 검증됨, 정책 도구
산출물 B: "지방에 온 사람의 체류 연장 코스"  ← 2·3단, 내국인 검증됨, 서비스 도구
```

이러면 각각 자기 데이터로 검증된 상태로 제시되고, 전이 가정이 필요 없다.
`통합분석` 문서의 **"소비/체재 배율 0.39"**(지방에 와도 돈을 안 쓴다) 문제를 B가 직접 겨냥한다는
서사도 성립한다.
