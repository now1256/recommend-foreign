# 외국인 지방 관광 추천 모델 — 대회 설명안

## 결론

처음 기획한 **“재방문 외국인이 이전에 방문한 서울 관광지를 입력하면, 취향에 맞는 지방 관광지를 추천”**은 현재 확보한 데이터로는 학습과 검증이 불가능하다.

그 이유는 다음 세 데이터가 서로 다른 단위로 존재하기 때문이다.

- 외래관광객조사: 외국인 개인 특성과 방문 **시도**는 있지만, 방문 POI 이력은 없음
- 신한카드: 외국인의 시군구별 소비는 있지만, 개인 ID와 방문 순서는 없음
- AI Hub 여행로그: POI 방문 이력은 있지만, 대상이 내국인이고 여행이 권역별로 분리됨

즉, 필요한 `외국인 개인 ID × 서울 POI × 지방 POI` 공동방문 데이터가 없다. AI Hub에서도 서울과 지방의 공동방문은 27쌍뿐이어서 “서울에서 어디를 갔는지”를 지방 추천의 학습 신호로 사용할 수 없다.

따라서 대회에서는 문제를 다음 두 개의 검증 가능한 모델로 나누는 것이 적절하다.

```text
[모델 1] 외국인 특성 → 지방 시도 추천
                         ↓ 선택된 시도
[모델 2] 현재 방문 POI → 같은 시도 안의 다음 POI 추천
                         ↓
                    지방 체류 코스 구성
```

핵심 설명은 다음과 같다.

> 모델 1은 외국인 데이터로 “어느 지방을 추천할 것인가”를 결정하고, 모델 2는 여행 행동 데이터로 “그 지방에서 어디를 함께 방문할 것인가”를 결정한다. 두 모델은 각각 검증했지만, 외국인 POI 정답 데이터가 없으므로 두 모델을 연결한 전체 성능은 주장하지 않는다.

---

## 최종 문제 정의

### 모델 1: 외국인 특성 기반 지방 시도 추천

- 입력: 국적, 연령, 재방문 여부, 개별여행 여부, 방한 목적, 체재기간, 방한 횟수, 동반자 수
- 후보: 부산·제주를 제외한 지방 12개 시도
- 출력: 사용자별 시도 추천 순위
- 학습 라벨: 외래관광객조사 문9-2의 시도별 실제 방문 여부
- 모델: 콘텐츠 기반 Two-Tower

부산·제주를 포함한 지방 14개를 먼저 점수화할 수 있지만, 정책적으로 이미 관문 역할을 하는 부산·제주를 제외한 **12개 지방 분산 추천 성능**을 핵심 지표로 제시한다.

### 모델 2: 방문 행동 기반 지방 POI 연관 추천

- 입력: 사용자가 현재 방문했거나 선택한 POI
- 후보: 선택된 시도 안의 지방 POI
- 출력: 함께 방문할 가능성이 높은 POI 순위
- 학습 라벨: AI Hub 여행로그의 사용자–POI 방문 여부
- 모델: EASE(Embarrassingly Shallow Autoencoder)

모델 2는 외국인 개인 특성을 학습하지 않는다. 실험 결과, 성별·연령·여행 성향이 같은 사용자끼리 같은 지역을 방문할 확률의 리프트가 1.10배에 불과했지만, 실제 방문 행동에는 강한 연관 신호가 있었기 때문이다.

---

## 실험에 사용한 데이터

### 모델 1 데이터: 2025년 외래관광객조사와 관문 변수

| 구분 | 사용 내용 |
|---|---|
| 원본 | `2025년_외래관광객조사_DATA_renamed.csv` |
| 전체 표본 | 외국인 응답자 16,110명, 408개 원변수 |
| 사용자 범주형 입력 | 국가별, 연령별, 방한횟수별에서 만든 재방문 여부, 여행형태별, 방한목적별 |
| 사용자 수치형 입력 | 총 체재기간, 방한 횟수, 동반자 수 |
| 정답 라벨 | 문9-2 방문 지역 14개: 부산, 제주, 강원, 충북, 충남, 세종, 대전, 전북, 전남, 광주, 경북, 대구, 경남, 울산 |
| 표본 가중치 | 외래관광객조사 `1~4분기 가중치` |
| 접근성 입력 | `logit_dataset.csv`의 국적×지역 직항 공급량 `supply`, 공항 입국 비중 `moj_share` |
| 지역 콘텐츠 입력 | AI Hub 관광지 유형 비중·만족도·체류시간·방문 선택 이유, 공공데이터 관광지·숙박·레저·휴양문화 통계 |
| 신한카드 입력 | `card_profile_gu.pkl`: 205개 시군구 × 업종별·국적별 소비 비중 71개 |

외래관광객조사의 원변수 중 모델에 실제로 들어가는 사용자 변수는 8개다. 범주형 5개는 임베딩하며 수치형 3개는 범위를 제한한 뒤 0~1로 스케일링한다.

| 변수 | 전처리 |
|---|---|
| 국적 | 21개 범주 임베딩 |
| 연령 | 6개 연령 범주 임베딩 |
| 재방문 | 방한 횟수 2회 이상이면 1 |
| 개별여행 | 여행형태가 개별여행이면 1 |
| 방한 목적 | 5개 범주 임베딩 |
| 체재기간 | 0~30일로 절단 후 30으로 나눔 |
| 방한 횟수 | 1~4 범위로 제한 후 4로 나눔 |
| 동반자 수 | 0~6명으로 절단 후 6으로 나눔 |

### 신한카드 소비 데이터의 역할

신한카드 데이터는 개인별 정답 라벨이나 손실함수의 표본 가중치가 아니다. 시군구가 어떤 외국인 소비 성격을 갖는지 나타내는 **지역 콘텐츠 피처**다.

| 카드 피처군 | 의미 |
|---|---|
| 업종별 소비 비중 | 호텔·숙박, 관광상품, 관광여행사, 쇼핑, 음식, 의료, 레저 등 지역 소비 구성 |
| 국적별 소비 비중 | 일본·중국·대만·미국·태국 등 20개 국적의 해당 지역 소비 구성 |

`card_profile_gu.pkl`의 형상은 205개 시군구 × 71개 변수다. AI Hub의 227개 시군구 중 201개가 카드 데이터와 정확히 매칭됐다. 시도 추천에서는 해당 시도에 속한 시군구 카드 피처의 평균을 사용한다.

$$
\mathbf{x}^{card}_{r}
=\frac{1}{|G_r|}\sum_{g\in G_r}\mathbf{x}^{card}_{g}
$$

- $G_r$: 시도 $r$에 속하면서 카드 데이터가 존재하는 시군구 집합
- $\mathbf{x}^{card}_{g}$: 시군구 $g$의 71차원 카드 소비 프로필

카드 피처는 지역의 다른 콘텐츠 피처와 이어 붙인 뒤 표준화해 지역 타워에 입력한다.

$$
\mathbf{x}_r=
\left[
\mathbf{x}^{AIHub}_r,
\mathbf{x}^{card}_r,
\mathbf{x}^{public}_r
\right]
$$

$$
\mathbf{v}_r=f_i(standardize(\mathbf{x}_r))
$$

따라서 카드 피처의 최종 영향력은 사람이 정한 고정 가중치가 아니라 지역 타워 신경망의 학습 파라미터로 결정된다. 외래관광객조사의 `1~4분기 가중치`는 표본 대표성을 보정하기 위해 손실함수에 곱하는 별도 값이다.

### 모델 2 데이터: AI Hub 국내 여행로그

| 구분 | 사용 내용 |
|---|---|
| 원본 | AI Hub 수도권·서부권·동부권 여행로그 8개 폴더의 방문지정보·여행정보 |
| 원본 관광형 방문 | 33,895건 |
| 전체 여행객 | 9,269명 |
| 원본 POI | 11,326개 |
| 전처리 | 관광형 방문만 유지하고 서울·경기·인천 제외 |
| 후보 필터 | 서로 다른 방문자가 10명 이상인 POI만 유지 |
| 최종 행렬 | 사용자 4,130명 × POI 342개 |
| 중복 제거 후 상호작용 | 8,027건 |
| 학습 입력 | 사용자–POI 이진 방문 행렬 |
| 사용하지 않는 변수 | 국적, 연령, 성별, 만족도, 카드 소비액 |

모델 2의 POI 키는 기존 실험과 수치를 재현하기 위해 `VISIT_AREA_NM`을 사용했다. 실제 서비스에서는 동명 관광지 충돌을 막기 위해 `시군구 + POI명` 복합키로 바꾸는 것이 안전하다.

---

## 모델 1 — 콘텐츠 기반 Two-Tower

### 왜 이 모델을 사용하는가

사용자마다 방문한 시도는 여러 개일 수 있으므로 이 문제는 단일 클래스 분류가 아니라 **사용자–지역 쌍의 방문 가능성을 구하는 다중 라벨 랭킹 문제**다.

또한 지역 ID 자체를 외우게 하면 인기 지역에 과도하게 쏠리고 새로운 지역으로 확장하기 어렵다. 따라서 사용자와 지역을 각각 특성 벡터로 표현한 뒤 동일한 잠재공간에서 적합도를 계산한다.

### 입력 표현

사용자 $u$의 범주형 변수는 각각 임베딩한 뒤 수치형 변수와 결합한다.

$$
\mathbf{x}_u =
\left[
E_{nat}(nat_u),
E_{age}(age_u),
E_{revisit}(revisit_u),
E_{fit}(fit_u),
E_{purpose}(purpose_u),
stay_u, nvisit_u, companion_u
\right]
$$

사용자 타워는 이 벡터를 잠재벡터 $\mathbf{z}_u$로 변환한다.

$$
\mathbf{z}_u = f_u(\mathbf{x}_u)
$$

지역 $r$은 카드 소비 특성, 관광지 유형 구성, 평균 만족도, 체류시간, 관광자원 등의 콘텐츠 특성 $\mathbf{x}_r$로 표현한다.

$$
\mathbf{v}_r = f_i(\mathbf{x}_r)
$$

여기서 구현상 사용자 타워와 지역 타워는 ReLU가 포함된 다층 신경망이며 잠재 차원은 128이다. 지역 ID 임베딩은 사용하지 않는다.

### 최종 점수

사용자–지역 기본 적합도는 두 잠재벡터의 내적이다.

$$
s^{pref}_{ur} = \frac{\mathbf{z}_u^\top \mathbf{v}_r}{\sqrt{d}}
$$

외국인은 국적별 직항편과 입국 관문의 영향을 크게 받으므로 접근성 변수를 별도 보정항으로 더한다.

$$
s_{ur} = s^{pref}_{ur}
+ \mathbf{w}_{acc}^{\top}
\begin{bmatrix}
\log(1+supply_{ur}) \\
moj\_share_{ur}
\end{bmatrix}
$$

- $supply_{ur}$: 해당 국적과 지역 배후공항 사이의 직항 공급
- $moj\_share_{ur}$: 해당 국적 입국자 중 해당 공항을 이용한 비중
- 점수가 클수록 사용자 $u$에게 지역 $r$을 우선 추천

### 학습 목적함수

실제 방문 여부를 $y_{ur}\in\{0,1\}$, 설문 가중치를 $w_u$라고 할 때 가중 이진 교차엔트로피를 최소화한다.

$$
\mathcal{L}_{region}
= -\frac{1}{N}
\sum_{u,r} w_u
\left[
y_{ur}\log\sigma(s_{ur})
+(1-y_{ur})\log(1-\sigma(s_{ur}))
\right]
$$

### 데이터 분할과 검증

`seed=0`으로 응답자 16,110명을 무작위 섞은 뒤 **사용자 단위 75:25 holdout**으로 나눴다. 한 응답자의 14개 지역 라벨 전체가 train 또는 test 한쪽에만 들어가므로 사용자 누수가 없다.

| 구분 | Train | Test |
|---|---:|---:|
| 전체 응답자 | 12,082명 | 4,028명 |
| 지방 14개 positive 라벨 | 5,334개 | 1,790개 |
| 지방 12개 positive 라벨 | 2,309개 | 776개 |
| 실제 평가 대상: 14개 중 1곳 이상 방문 | - | 1,351명 |
| 실제 평가 대상: 12개 중 1곳 이상 방문 | - | 658명 |

모델 학습에는 모든 사용자–지역 쌍을 사용한다. 14개 후보 기준으로 train 행은 `12,082 × 14 = 169,148개`다. 평가는 해당 후보군에 실제 방문 정답이 하나 이상 있는 test 사용자에게만 계산한다.

하이퍼파라미터는 잠재 차원 128, 범주 임베딩 차원 12, 사용자 어댑터 출력 96, 은닉층 128, dropout 0.1, batch 4,096, Adam 최적화, 학습률 0.002, weight decay $10^{-5}$, 20 epoch, 설문 가중치 적용이다. 카드 포함 지역 입력은 총 96차원이며 이 중 71차원이 카드 소비 피처다.

### 평가 지표

Recall@k는 실제 방문지역 중 하나라도 추천 상위 $k$개 안에 포함되면 적중으로 계산한다.

$$
Recall@k
= \frac{1}{|U^+|}
\sum_{u\in U^+}
\mathbb{1}
\left(
TopK(s_u)\cap Y_u^+ \neq \varnothing
\right)
$$

인기순 기준선은 train 데이터에서 각 지역의 방문 비율을 구한 뒤 모든 test 사용자에게 동일한 순위를 제공한다. 이 기준선과 비교해야 모델이 단순 지역 인기를 넘어 개인별 순위를 만들었는지 알 수 있다.

### 2026-08-30 재실행 결과

| 학습 방식 | 평가 범위 | R@1 | R@3 | R@5 | R@10 | Top-1 지역 종류 |
|---|---|---:|---:|---:|---:|---:|
| 인기순 | 지방 14개 | 0.5085 | 0.8268 | 0.8564 | 0.9563 | 1 |
| 외국인 직접학습·카드 미포함 | 지방 14개 | 0.6181 | 0.8261 | 0.8845 | **0.9637** | - |
| **외국인 직접학습·카드 포함** | **지방 14개** | **0.6225** | **0.8327** | **0.8860** | 0.9630 | - |
| 인기순 | 지방 12개 | 0.3419 | 0.5927 | 0.7158 | **0.9635** | 1 |
| 외국인 직접학습·카드 미포함 | 지방 12개 | 0.3693 | 0.6520 | 0.7675 | 0.9590 | 7 / 12 |
| **외국인 직접학습·카드 포함** | **지방 12개** | **0.4058** | **0.6763** | **0.7994** | **0.9757** | 6 / 12 |
| 내국인 사전학습 후 외국인 전이·카드 포함 | 지방 12개 | 0.2857 | 0.5152 | 0.6869 | 0.9240 | 7 / 12 |

핵심 비교는 지방 12개의 카드 포함 외국인 직접학습 Two-Tower와 인기순이다. R@1은 0.3419에서 0.4058로 0.0639p, R@3은 0.5927에서 0.6763으로 0.0836p 상승했다.

카드 ablation에서는 R@1이 0.3693에서 0.4058로 0.0365p, R@3이 0.6520에서 0.6763으로 0.0243p 상승했다. 따라서 신한카드 소비 피처가 외국인 지방 시도 랭킹에 추가 신호를 제공했다고 해석할 수 있다. 반면 내국인 사전학습 전이는 카드 포함 상태에서도 인기순보다 낮아 최종 모델에서 제외한다.



---

## 모델 2 — EASE 기반 POI 연관 추천

### 왜 개인 특성 모델 대신 EASE를 사용하는가

AI Hub 지방 여행 데이터는 사용자 1명당 방문 POI가 평균 약 3개로 매우 희소하다. 사용자 임베딩을 학습하는 MF·NeuMF는 사용자별 관측치가 부족해 과적합했지만, 10명 이상 방문한 POI로 제한하면 아이템 간 공동방문 관계는 비교적 안정적으로 계산할 수 있었다.

따라서 “이런 사람은 어디를 좋아하는가”가 아니라 **“이 장소를 방문한 사람은 또 어디를 함께 방문했는가”**를 학습한다.

### 사용자–POI 행렬

사용자 $u$가 POI $i$를 방문했으면 1, 아니면 0인 행렬 $R$을 만든다.

$$
R_{ui} =
\begin{cases}
1, & \text{사용자 }u\text{가 POI }i\text{를 방문} \\
0, & \text{그 외}
\end{cases}
$$

EASE는 자기 자신을 이용해 자기 자신을 예측하지 못하도록 대각 원소를 0으로 제한하면서 다음 문제를 푼다.

$$
\min_B \|R-RB\|_F^2 + \lambda\|B\|_F^2,
\qquad diag(B)=0
$$

- 첫 번째 항: 실제 방문 행렬과 복원된 방문 점수의 차이
- 두 번째 항: 관계 가중치가 지나치게 커지는 것을 막는 L2 정규화
- 실험에서 $\lambda=500$ 사용

### 닫힌형 해

신경망처럼 에폭을 반복해 학습할 필요 없이 행렬 연산으로 해를 한 번에 구한다.

$$
G=R^\top R+\lambda I
$$

$$
P=G^{-1}
$$

$$
B_{ij}=\frac{P_{ij}}{-P_{jj}},\qquad B_{jj}=0
$$

최종 추천 점수는 다음과 같다.

$$
S=RB
$$

현재 사용자의 방문 벡터를 $\mathbf{r}_u$라고 하면 후보 POI $j$의 점수는 다음과 같이 해석할 수 있다.

$$
s_{uj}=\sum_{i\in I_u}R_{ui}B_{ij}
$$

즉, 이미 방문한 모든 POI와 후보 POI의 학습된 연관도를 합산한다. 이 방식은 “직전 장소 하나”만 보는 것이 아니라 현재까지 선택한 장소 집합 전체를 반영한다.

### 데이터 분할과 검증

후보 POI를 2개 이상 방문한 사용자 2,094명을 평가 대상으로 정했다. 각 사용자에서 `seed=0`으로 POI 한 곳을 무작위로 숨겨 test 정답으로 사용하고, 나머지 방문은 train에 남겼다. 후보 POI가 1개뿐인 사용자는 정답을 숨기면 입력이 없어지므로 평가에서는 제외하되 EASE의 공동방문 행렬 학습에는 사용했다.

| 항목 | 값 |
|---|---:|
| 필터 후 전체 사용자 | 4,130명 |
| 후보 POI | 342개 |
| holdout 전 상호작용 | 8,027건 |
| train 상호작용 | 5,933건 |
| test 상호작용 | 2,094건 |
| 평가 사용자 | 2,094명 |

아이템 최소 방문자 10명 필터는 holdout 전에 적용했다. 따라서 이는 고정된 후보 카탈로그 안에서의 추천 성능이며, 한 번도 관측되지 않은 신규 POI의 cold-start 성능은 평가하지 않는다. 이미 train에서 방문한 POI는 추천 점수를 $-\infty$로 마스킹해 재추천하지 않는다.

### 평가 지표

$$
HR@k
=\frac{1}{|U_{eval}|}
\sum_{u\in U_{eval}}
\mathbb{1}(i_u^{holdout}\in TopK(s_u))
$$

사용자의 방문지 하나를 정답으로 숨긴 뒤, 나머지 방문지만으로 숨긴 장소가 상위 $k$개에 등장하는지를 측정한다. 인기순 기준선은 train에서 POI별 방문 횟수를 계산해 모든 사용자에게 동일하게 추천한다.

### 2026-08-30 재실행 결과

전체 비수도권 POI 342개를 후보로 평가한 결과다.

| 모델 | HR@1 | HR@5 | HR@10 | HR@20 |
|---|---:|---:|---:|---:|
| 인기순 | 0.0291 | 0.0998 | 0.1552 | 0.2483 |
| **EASE($\lambda=500$)** | **0.2130** | **0.5396** | **0.6762** | **0.7708** |

HR@10은 인기순 0.1552 대비 EASE 0.6762로 약 **4.36배**, HR@1은 약 **7.32배**다. 기존 문서의 EASE HR@10 0.678과 거의 동일해 재현성이 확인됐다.

모델 1이 정답 시도를 골랐다고 가정하고 해당 시도 안의 POI만 후보로 제한한 조건부 결과도 계산했다.

| 모델 | HR@1 | HR@5 | HR@10 | HR@20 |
|---|---:|---:|---:|---:|
| 시도 내 인기순 | 0.1590 | 0.4207 | 0.5750 | 0.7445 |
| **시도 내 EASE** | **0.2230** | **0.5697** | **0.7163** | **0.8524** |

이 조건부 평가는 모델 1이 시도를 맞혔다는 가정 아래 모델 2만 측정한 값이다. 두 모델을 연쇄 실행한 end-to-end 성능이 아니다. 또한 모델 2의 정답은 내국인 AI Hub 여행로그이므로 외국인 POI 추천 성능으로 표현하면 안 된다.

---

## 두 모델을 서비스에서 연결하는 방법

### 4.1 추천 흐름

1. 외국인의 국적·연령·재방문 여부·여행 목적 등을 모델 1에 입력한다.
2. 지방 12개 시도의 점수를 계산해 상위 3개를 제안한다.
3. 사용자가 시도 하나를 선택한다.
4. 해당 시도 안에서 인기와 접근성이 높은 시작 POI를 제안하거나 사용자가 시작 POI를 직접 고른다.
5. 선택된 POI를 모델 2에 입력해 관련 POI를 순차적으로 추가한다.
6. 거리와 관광지 유형을 후처리 조건으로 적용해 실제 이동 가능한 코스로 정렬한다.

### 4.2 코스 생성용 후처리 점수

코스 후보 $i$의 점수는 다음처럼 구성할 수 있다.

$$
routeScore(i\mid C)=
2.0\cdot rel_{EASE}(i,C)
+1.0\cdot proximity(i,C)
+0.5\cdot diversity(i,C)
+0.1\cdot satisfaction_i
$$

여기서

$$
rel_{EASE}(i,C)=\frac{1}{|C|}\sum_{j\in C}B_{ji}
$$

$$
proximity(i,C)=\max\left(0,1-\frac{d(i,C)}{25\text{ km}}\right)
$$

- $C$: 지금까지 선택된 POI 집합
- $d(i,C)$: 후보와 현재 코스 사이의 거리
- $diversity$: 코스에 아직 없는 관광 유형이면 1, 이미 있으면 0
- 매 단계에서 점수가 가장 높은 후보를 하나씩 추가하는 탐욕 방식

단, 이 코스 점수의 계수 `2.0, 1.0, 0.5, 0.1`은 현재 실험으로 최적화된 값이 아니라 **서비스 설계용 규칙값**이다. 대회에서는 “EASE 모델의 성능”과 “거리·다양성 후처리 규칙”을 구분해서 설명해야 한다.

---

## 실험 실행 방법과 결과 파일

실험 환경은 macOS ARM64, Python 3.12.0, CPU이며 저장소의 `.venv`에 `requirements.txt`를 설치했다. 주요 라이브러리는 PyTorch 2.13.0, NumPy 2.5.2, pandas 3.0.5, scikit-learn 1.9.0, SciPy 1.18.1이다.

```bash
# 최초 1회
python -m venv .venv
.venv/bin/pip install -r requirements.txt

# 모델 1: 내국인 사전학습, 외국인 직접학습 대조군, 전이학습을 순서대로 실행
.venv/bin/python -m src.train all

# 모델 2: EASE leave-one-out 평가
.venv/bin/python -m src.ease_experiment
```

| 산출물 | 내용 |
|---|---|
| `artifacts/results.json` | Two-Tower 사전학습·직접학습·전이학습 결과 |
| `artifacts/pretrained.pt` | 내국인 사전학습 체크포인트와 아이템 피처 열 정보 |
| `artifacts/ease_results.json` | EASE 전체 지방 및 시도 조건부 평가 결과 |
| `artifacts/card_profile_gu.pkl` | Two-Tower에 사용한 시군구별 신한카드 소비 프로필 |

재실행 과정에서 두 가지 코드 정합성 문제를 수정했다.

1. AI Hub 권역별 CSV에서 숫자와 문자열로 섞인 `TRAVELER_ID`를 문자열로 통일했다.
2. 시군구 콘텐츠 피처를 시도 추천에 사용할 때 해당 시도의 시군구 평균으로 집계하고, 사전학습과 파인튜닝에서 아이템 피처 열 순서를 동일하게 유지했다.
3. 신한카드 캐시를 지역 콘텐츠 행렬에 결합해 카드 포함 실험을 동일한 split과 seed로 다시 실행했다.

## 실험 결과 해석

- 모델 1의 최종 후보는 **외국인 조사로 직접 학습하고 신한카드 지역 소비 피처를 포함한 Two-Tower**다. 지방 12개에서 R@1 0.4058, R@3 0.6763으로 인기순을 넘었다.
- 카드 미포함 대비 카드 포함 실험에서 R@1이 0.0365p, R@3이 0.0243p 상승해 카드 소비 프로필의 추가 기여가 확인됐다.
- 내국인 시군구 데이터 사전학습은 외국인 시도 추천으로 전이되지 않았다. 따라서 최종 모델 설명에서 사전학습 성능을 장점으로 주장하지 않는다.
- 모델 2 EASE는 기존 실험 결과를 재현했다. 전체 지방 HR@10은 0.6762로 인기순 0.1552보다 크게 높다.
- EASE는 신경망이 아니라 정규화된 선형 협업 필터링 모델이다. 데이터가 희소해 복잡한 신경망보다 이 구조가 더 적합하다.
- 모델 1은 외국인 시도 라벨로 검증됐고 모델 2는 내국인 POI 라벨로 검증됐다. 두 모델의 end-to-end 외국인 성능은 현재 데이터로 측정할 수 없다.
- 코스 생성의 거리·다양성 계수는 아직 검증된 모델 파라미터가 아니라 서비스 후처리 규칙이다.
